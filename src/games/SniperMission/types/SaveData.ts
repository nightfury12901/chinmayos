export interface SaveData {
  credits: number;
  xp: number;
  equippedWeapon: string;
  unlockedWeapons: string[];
  unlockedMaps: string[];
  missionsCompleted: number;
  totalKills: number;
  bestScore: number;
}
